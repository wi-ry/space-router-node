# macOS Installation Guide

## Installer Package
1. Download the `.pkg` from GitHub Releases.
2. Open the installer and follow the prompts.
3. The service will be managed via `launchd`.
4. Configuration is located at `~/.spacerouter/spacerouter.env`.
